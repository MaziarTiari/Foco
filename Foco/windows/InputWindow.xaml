<Window x:Class="Foco.windows.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Foco.windows"
        mc:Ignorable="d"
        Title="InputTitle" WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" Closing="OnWindowClosing" FontFamily="Century Gothic">

    <StackPanel Margin="10">
        <DockPanel>
            <Label Name="InputLabel" Content="Label:" Width="50"/>
            <TextBox Name="InputBox" KeyDown="OnKeyDown" MaxLines="1" Height="23" TextWrapping="NoWrap" Text="DefaultText" Width="160" MaxLength="30"/>
        </DockPanel>
        <DockPanel Margin="0 20 0 0">
            <Button DockPanel.Dock="Right" Padding="4" Content="Speichern" Tag="Save" Name="SaveButton" Width="80" Click="OnButtonClicked"/>
            <Button DockPanel.Dock="Right" Padding="4" Content="Abbrechen" Tag="Cancel" HorizontalAlignment="Right" Margin="0 0 5 0" Width="80" Click="OnButtonClicked"/>
        </DockPanel>
    </StackPanel>

</Window>
